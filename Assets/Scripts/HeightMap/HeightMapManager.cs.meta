using UnityEngine;
using System;
using DRE.Noise;


public class HeightMapTileManager : MonoBehaviour
{
    private INoise noise;
    static ulong noiseSeed = (ulong)Environment.TickCount;

    void Start()
    {
        INoise simplexNoise = new SimplexNoise(noiseSeed);

        // Variant: standard
        noise = new LinearSimplexNoiseGenerator(simplexNoise);

        // Variant: exponential noise
        //noise = new ExponentialSimplexNoiseGenerator(simplexNoise);

        // Variant: Checker board
        //INoise n1 = new CheckerBoardNoise();
        //INoise n2 = new ExponentialSimplexNoiseGenerator(simplexNoise);
        //noise = new NoiseAdd(n1, n2, 0.2);


        //mesh = GetComponent<MeshFilter>().mesh;
    }

}
